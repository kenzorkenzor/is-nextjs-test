import {
    Alert,
    AlertDescription,
    AlertTitle,
} from '@/components/ui/alert'

export default function AdminHome() {
    return (
        <section className="flex min-h-screen flex-col items-center justify-between p-24">
            <Alert>
                <AlertTitle>Admin Home</AlertTitle>
                <AlertDescription>
                </AlertDescription>
            </Alert>
        </section>
    )
}
